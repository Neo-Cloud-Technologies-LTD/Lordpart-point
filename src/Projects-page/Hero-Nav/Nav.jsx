// The site now uses one shared navbar rendered by <Layout /> (src/Components/Navbar.jsx).
// Nav.css was intentionally dropped here because it styled the global `nav` element
// selector, which leaked into every other page's navigation.
export { default } from "../../Components/Navbar";
